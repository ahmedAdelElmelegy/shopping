import 'package:flutter/material.dart';
import 'package:shopping_app/core/assets/image/assets.dart';

class ChangePasswordImage extends StatelessWidget {
  const ChangePasswordImage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(Assets.imagesChangePassword);
  }
}
